[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "western-hognoose"
version = "0.1.0"
description = "Reusable helper utilities"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/disk_dict.py", "src/output_file.py"]
